name: Release

on:
  push:
    branches:
      - main

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: false

permissions:
  contents: write
  pull-requests: write

jobs:
  release:
    name: Create release pull request
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Enable Corepack
        run: corepack enable

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Build workspace packages
        run: |
         pnpm --filter @jamit/audio-recorder-core build
         pnpm --filter @jamit/audio-recorder-vue build
         pnpm --filter @jamit/audio-recorder-react build
         pnpm --filter @jamit/audio-recorder-nuxt build

      - name: Prepare Nuxt example
        run: pnpm --filter @jamit/audio-recorder-example-nuxt exec nuxt prepare

      - name: Typecheck
        run: pnpm typecheck

      - name: Test
        run: pnpm test

      - name: Build
        run: pnpm build

      - name: Create release pull request
        uses: changesets/action@v1
        with:
          version: pnpm version-packages
          title: "chore: release packages"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}